 <div class="recipe-form">
{{ form_start(form) }}
 <h1 class= "text-center">Ajouter une nouvelle recette</h1>
    {{ form_widget(form) }}

<div class= "d-flex justify-content-center">
    <button class="btn btn-primary my-1">{{ button_label|default('Enregistrer') }}</button>
</div>
    
{{ form_end(form) }}
</div>